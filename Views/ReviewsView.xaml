<UserControl x:Class="QalatAldhaman.Store.Admin.Views.ReviewsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             FontFamily="Segoe UI">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="الآراء"
                   FontSize="22"
                   FontWeight="Bold"
                   Margin="0,0,0,16" />

        <CheckBox Grid.Row="1"
                  Content="غير المعتمدة فقط"
                  IsChecked="{Binding PendingOnly, Mode=TwoWay}"
                  Margin="0,0,0,16" />

        <Grid Grid.Row="2">
            <TextBlock Text="جارِ التحميل..."
                       Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontSize="16"
                       Foreground="#666" />

            <StackPanel Visibility="{Binding IsError, Converter={StaticResource BoolToVis}}"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center">
                <TextBlock Text="{Binding ErrorMessage}"
                           Foreground="#C0392B"
                           Margin="0,0,0,12"
                           TextAlignment="Center"
                           TextWrapping="Wrap"
                           MaxWidth="400" />
                <Button Content="إعادة المحاولة"
                        Command="{Binding LoadCommand}"
                        Padding="16,8"
                        HorizontalAlignment="Center" />
            </StackPanel>

            <TextBlock Text="لا توجد آراء"
                       Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVis}}"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontSize="16"
                       Foreground="#888" />

            <DataGrid ItemsSource="{Binding Reviews}"
                      Visibility="{Binding HasReviews, Converter={StaticResource BoolToVis}}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      RowHeight="44">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="الزبون" Binding="{Binding CustomerName}" Width="130" />
                    <DataGridTextColumn Header="المنتج" Binding="{Binding ProductName}" Width="150" />
                    <DataGridTextColumn Header="التقييم" Binding="{Binding RatingText}" Width="110" />
                    <DataGridTextColumn Header="الرأي" Binding="{Binding Comment}" Width="*" />
                    <DataGridTextColumn Header="الحالة" Binding="{Binding StatusText}" Width="130" />
                    <DataGridTemplateColumn Header="إجراءات" Width="170">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="موافقة"
                                            Margin="4,2"
                                            Padding="10,4"
                                            Visibility="{Binding IsApproveVisible, Converter={StaticResource BoolToVis}}"
                                            Command="{Binding DataContext.ApproveCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}" />
                                    <Button Content="حذف"
                                            Margin="4,2"
                                            Padding="10,4"
                                            Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}" />
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>

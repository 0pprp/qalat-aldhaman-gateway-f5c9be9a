<UserControl x:Class="QalatAldhaman.Store.Admin.Views.OrdersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             FontFamily="Segoe UI">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="الطلبات"
                   FontSize="22"
                   FontWeight="Bold"
                   Margin="0,0,0,16" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="الحالة:" VerticalAlignment="Center" Margin="0,0,8,0" />
            <ComboBox Width="170"
                      Margin="0,0,20,0"
                      ItemsSource="{Binding StatusOptions}"
                      DisplayMemberPath="Label"
                      SelectedItem="{Binding SelectedStatusFilter, Mode=TwoWay}" />

            <TextBlock Text="الفئة:" VerticalAlignment="Center" Margin="0,0,8,0" />
            <ComboBox Width="170"
                      Margin="0,0,20,0"
                      ItemsSource="{Binding Categories}"
                      DisplayMemberPath="Name"
                      SelectedItem="{Binding SelectedCategoryFilter, Mode=TwoWay}" />

            <TextBlock Text="طريقة الدفع:" VerticalAlignment="Center" Margin="0,0,8,0" />
            <ComboBox Width="170"
                      ItemsSource="{Binding PurchaseMethodOptions}"
                      DisplayMemberPath="Label"
                      SelectedItem="{Binding SelectedPurchaseMethodFilter, Mode=TwoWay}" />
        </StackPanel>

        <Grid Grid.Row="2">
            <TextBlock Text="جارِ التحميل..."
                       Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontSize="16"
                       Foreground="#666" />

            <StackPanel Visibility="{Binding IsError, Converter={StaticResource BoolToVis}}"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center">
                <TextBlock Text="{Binding ErrorMessage}"
                           Foreground="#C0392B"
                           Margin="0,0,0,12"
                           TextAlignment="Center"
                           TextWrapping="Wrap"
                           MaxWidth="400" />
                <Button Content="إعادة المحاولة"
                        Command="{Binding LoadCommand}"
                        Padding="16,8"
                        HorizontalAlignment="Center" />
            </StackPanel>

            <TextBlock Text="لا توجد طلبات"
                       Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVis}}"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontSize="16"
                       Foreground="#888" />

            <DataGrid ItemsSource="{Binding Orders}"
                      Visibility="{Binding HasOrders, Converter={StaticResource BoolToVis}}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      RowHeight="40">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="رقم الطلب" Binding="{Binding OrderNumber}" Width="120" />
                    <DataGridTextColumn Header="الزبون" Binding="{Binding CustomerName}" Width="140" />
                    <DataGridTextColumn Header="المنتج" Binding="{Binding ProductName}" Width="*" />
                    <DataGridTextColumn Header="طريقة الدفع" Binding="{Binding PurchaseMethodText}" Width="140" />
                    <DataGridTemplateColumn Header="الحالة" Width="140">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding StatusText}"
                                           Foreground="{Binding StatusColor}"
                                           FontWeight="Bold"
                                           VerticalAlignment="Center" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="التاريخ" Binding="{Binding CreatedAtText}" Width="140" />
                    <DataGridTemplateColumn Header="" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="تفاصيل"
                                        Margin="4,2"
                                        Padding="10,4"
                                        Command="{Binding DataContext.OpenOrderCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>

<UserControl x:Class="QalatAldhaman.Store.Admin.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             FontFamily="Segoe UI">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
        <Style x:Key="Card" TargetType="Border" BasedOn="{StaticResource CardBorderStyle}">
            <Setter Property="Margin" Value="0,0,12,12" />
        </Style>
        <Style x:Key="CardTitle" TargetType="TextBlock" BasedOn="{StaticResource CaptionTextStyle}">
            <Setter Property="Margin" Value="0,0,0,6" />
        </Style>
        <Style x:Key="CardValue" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Foreground" Value="{DynamicResource BrandTextPrimaryBrush}" />
        </Style>
        <Style x:Key="CardIcon" TargetType="materialDesign:PackIcon">
            <Setter Property="Width" Value="26" />
            <Setter Property="Height" Value="26" />
            <Setter Property="Foreground" Value="{StaticResource BrandPrimaryBrush}" />
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="Margin" Value="0,0,0,8" />
        </Style>
        <Style x:Key="SectionTitle" TargetType="TextBlock" BasedOn="{StaticResource SubheaderTextStyle}">
            <Setter Property="Margin" Value="0,8,0,12" />
        </Style>
    </UserControl.Resources>
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock DockPanel.Dock="Right"
                       Text="الإحصائيات"
                       Style="{StaticResource HeaderTextStyle}"
                       VerticalAlignment="Center" />
            <Button DockPanel.Dock="Left"
                    Content="تحديث"
                    Command="{Binding LoadCommand}" />
        </DockPanel>

        <Grid Grid.Row="1">
            <TextBlock Text="جارِ التحميل..."
                       Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontSize="16"
                       Foreground="{DynamicResource BrandTextSecondaryBrush}" />

            <StackPanel Visibility="{Binding IsError, Converter={StaticResource BoolToVis}}"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center">
                <TextBlock Text="{Binding ErrorMessage}"
                           Foreground="{StaticResource DangerBrush}"
                           Margin="0,0,0,12"
                           TextAlignment="Center"
                           TextWrapping="Wrap"
                           MaxWidth="400" />
                <Button Content="إعادة المحاولة"
                        Command="{Binding LoadCommand}"
                        HorizontalAlignment="Center" />
            </StackPanel>

            <ScrollViewer Visibility="{Binding IsContentVisible, Converter={StaticResource BoolToVis}}"
                          VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <!-- بطاقات علوية: الإجمالي + آخر 7/30 يوم + القيمة التقديرية -->
                    <WrapPanel Orientation="Horizontal">
                        <Border Style="{StaticResource Card}" Width="180">
                            <StackPanel>
                                <materialDesign:PackIcon Kind="ClipboardListOutline" Style="{StaticResource CardIcon}" />
                                <TextBlock Text="إجمالي الطلبات" Style="{StaticResource CardTitle}" />
                                <TextBlock Text="{Binding TotalOrders}" Style="{StaticResource CardValue}" />
                            </StackPanel>
                        </Border>
                        <Border Style="{StaticResource Card}" Width="180">
                            <StackPanel>
                                <materialDesign:PackIcon Kind="CalendarWeekOutline" Style="{StaticResource CardIcon}" />
                                <TextBlock Text="طلبات آخر 7 أيام" Style="{StaticResource CardTitle}" />
                                <TextBlock Text="{Binding OrdersLast7Days}" Style="{StaticResource CardValue}" />
                            </StackPanel>
                        </Border>
                        <Border Style="{StaticResource Card}" Width="180">
                            <StackPanel>
                                <materialDesign:PackIcon Kind="CalendarMonthOutline" Style="{StaticResource CardIcon}" />
                                <TextBlock Text="طلبات آخر 30 يوم" Style="{StaticResource CardTitle}" />
                                <TextBlock Text="{Binding OrdersLast30Days}" Style="{StaticResource CardValue}" />
                            </StackPanel>
                        </Border>
                        <Border Style="{StaticResource Card}" Width="250">
                            <StackPanel>
                                <materialDesign:PackIcon Kind="CashMultiple" Style="{StaticResource CardIcon}" Foreground="{StaticResource BrandAccentBrush}" />
                                <TextBlock Text="إجمالي قيمة الطلبات المؤكدة" Style="{StaticResource CardTitle}" />
                                <TextBlock Text="{Binding EstimatedConfirmedRevenueText}" Style="{StaticResource CardValue}" FontSize="18" />
                                <TextBlock Text="تقديري (سعر وقت الطلب، وليس المُحصَّل فعلياً)" Style="{StaticResource CaptionTextStyle}" FontSize="10" Margin="0,4,0,0" TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>
                        <Border Style="{StaticResource Card}" Width="160">
                            <StackPanel>
                                <materialDesign:PackIcon Kind="StarOutline" Style="{StaticResource CardIcon}" Foreground="{StaticResource StatusPendingBrush}" />
                                <TextBlock Text="آراء بانتظار الموافقة" Style="{StaticResource CardTitle}" />
                                <TextBlock Text="{Binding PendingReviewsCount}" Style="{StaticResource CardValue}" />
                            </StackPanel>
                        </Border>
                        <Border Style="{StaticResource Card}" Width="160">
                            <StackPanel>
                                <materialDesign:PackIcon Kind="PackageVariantClosed" Style="{StaticResource CardIcon}" />
                                <TextBlock Text="منتجات فعّالة" Style="{StaticResource CardTitle}" />
                                <TextBlock Text="{Binding ActiveProductsCount}" Style="{StaticResource CardValue}" />
                            </StackPanel>
                        </Border>
                        <Border Style="{StaticResource Card}" Width="160">
                            <StackPanel>
                                <materialDesign:PackIcon Kind="ShapeOutline" Style="{StaticResource CardIcon}" />
                                <TextBlock Text="فئات فعّالة" Style="{StaticResource CardTitle}" />
                                <TextBlock Text="{Binding ActiveCategoriesCount}" Style="{StaticResource CardValue}" />
                            </StackPanel>
                        </Border>
                    </WrapPanel>

                    <!-- بطاقات الحالات -->
                    <TextBlock Text="الطلبات حسب الحالة" Style="{StaticResource SectionTitle}" />
                    <ItemsControl ItemsSource="{Binding StatusCounts}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource Card}" Width="150">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Label}" Style="{StaticResource CardTitle}" />
                                        <TextBlock Text="{Binding Count}" Style="{StaticResource CardValue}" Foreground="{Binding Color}" />
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <!-- توزيعات -->
                    <Grid Margin="0,8,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="1" Style="{StaticResource CardBorderStyle}" Margin="0,0,20,12">
                            <StackPanel>
                                <TextBlock Text="توزيع الطلبات حسب الفئة" Style="{StaticResource SectionTitle}" Margin="0,0,0,8" />
                                <ItemsControl ItemsSource="{Binding CategoryCounts}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <DockPanel Margin="0,0,0,8">
                                                <TextBlock DockPanel.Dock="Left" Text="{Binding Count}" FontWeight="Bold" Foreground="{StaticResource BrandPrimaryBrush}" />
                                                <TextBlock Text="{Binding Label}" Margin="0,0,12,0" />
                                            </DockPanel>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>

                        <Border Grid.Column="0" Style="{StaticResource CardBorderStyle}" Margin="0,0,0,12">
                            <StackPanel>
                                <TextBlock Text="توزيع الطلبات حسب طريقة الدفع" Style="{StaticResource SectionTitle}" Margin="0,0,0,8" />
                                <ItemsControl ItemsSource="{Binding PurchaseMethodCounts}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <DockPanel Margin="0,0,0,8">
                                                <TextBlock DockPanel.Dock="Left" Text="{Binding Count}" FontWeight="Bold" Foreground="{StaticResource BrandPrimaryBrush}" />
                                                <TextBlock Text="{Binding Label}" Margin="0,0,12,0" />
                                            </DockPanel>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <!-- آخر 10 طلبات -->
                    <TextBlock Text="آخر 10 طلبات" Style="{StaticResource SectionTitle}" />
                    <DataGrid ItemsSource="{Binding RecentOrders}"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              CanUserAddRows="False"
                              HeadersVisibility="Column"
                              MaxHeight="320">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="رقم الطلب" Binding="{Binding OrderNumber}" Width="120" />
                            <DataGridTextColumn Header="الزبون" Binding="{Binding CustomerName}" Width="140" />
                            <DataGridTextColumn Header="المنتج" Binding="{Binding ProductName}" Width="*" />
                            <DataGridTemplateColumn Header="الحالة" Width="130">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding StatusText}" Foreground="{Binding StatusColor}" FontWeight="Bold" VerticalAlignment="Center" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="التاريخ" Binding="{Binding CreatedAtText}" Width="140" />
                            <DataGridTemplateColumn Header="" Width="90">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="تفاصيل"
                                                Margin="4,2"
                                                Padding="10,4"
                                                Command="{Binding DataContext.OpenOrderCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                CommandParameter="{Binding}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>

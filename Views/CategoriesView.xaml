<UserControl x:Class="QalatAldhaman.Store.Admin.Views.CategoriesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             FontFamily="Segoe UI">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock DockPanel.Dock="Right"
                       Text="الفئات"
                       FontSize="22"
                       FontWeight="Bold"
                       VerticalAlignment="Center" />
            <Button DockPanel.Dock="Left"
                    Content="+ إضافة فئة جديدة"
                    Command="{Binding AddCategoryCommand}"
                    Padding="16,8"
                    Background="#1F5F5B"
                    Foreground="White"
                    BorderThickness="0" />
        </DockPanel>

        <Grid Grid.Row="1">
            <TextBlock Text="جارِ التحميل..."
                       Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontSize="16"
                       Foreground="#666" />

            <StackPanel Visibility="{Binding IsError, Converter={StaticResource BoolToVis}}"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center">
                <TextBlock Text="{Binding ErrorMessage}"
                           Foreground="#C0392B"
                           Margin="0,0,0,12"
                           TextAlignment="Center"
                           TextWrapping="Wrap"
                           MaxWidth="400" />
                <Button Content="إعادة المحاولة"
                        Command="{Binding LoadCommand}"
                        Padding="16,8"
                        HorizontalAlignment="Center" />
            </StackPanel>

            <TextBlock Text="لا توجد فئات بعد"
                       Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVis}}"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontSize="16"
                       Foreground="#888" />

            <DataGrid ItemsSource="{Binding Categories}"
                      Visibility="{Binding HasCategories, Converter={StaticResource BoolToVis}}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      RowHeight="40">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="الاسم" Binding="{Binding Name}" Width="*" />
                    <DataGridTextColumn Header="طرق الدفع المسموحة" Binding="{Binding PaymentMethodsSummary}" Width="180" />
                    <DataGridTextColumn Header="صاحب محل؟" Binding="{Binding RequiresShopOwnerText}" Width="100" />
                    <DataGridTextColumn Header="الحالة" Binding="{Binding StatusText}" Width="90" />
                    <DataGridTemplateColumn Header="إجراءات" Width="170">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="تعديل"
                                            Margin="4,2"
                                            Padding="10,4"
                                            Command="{Binding DataContext.EditCategoryCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}" />
                                    <Button Content="حذف"
                                            Margin="4,2"
                                            Padding="10,4"
                                            Command="{Binding DataContext.DeleteCategoryCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}" />
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>

<Window x:Class="QalatAldhaman.Store.Admin.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="تسجيل الدخول — لوحة تحكم قلعة الضمان"
        Height="460" Width="380"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        FlowDirection="RightToLeft"
        FontFamily="Segoe UI">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </Window.Resources>
    <Grid Margin="32">
        <StackPanel VerticalAlignment="Center" IsEnabled="{Binding IsLoginEnabled}">
            <TextBlock Text="قلعة الضمان"
                       FontSize="22"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Foreground="#1F5F5B"
                       Margin="0,0,0,4" />
            <TextBlock Text="لوحة تحكم الأدمن"
                       FontSize="13"
                       HorizontalAlignment="Center"
                       Foreground="#666666"
                       Margin="0,0,0,24" />

            <TextBlock Text="اسم المستخدم" Margin="0,0,0,4" />
            <TextBox x:Name="UsernameInput"
                     Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                     Padding="8"
                     FontSize="14"
                     Margin="0,0,0,12" />

            <TextBlock Text="كلمة المرور" Margin="0,0,0,4" />
            <PasswordBox x:Name="PasswordInput"
                         Padding="8"
                         FontSize="14"
                         Margin="0,0,0,20"
                         KeyDown="PasswordInput_KeyDown" />

            <Button Content="دخول"
                    Click="LoginButton_Click"
                    Height="40"
                    FontSize="15"
                    Background="#1F5F5B"
                    Foreground="White"
                    BorderThickness="0" />

            <TextBlock Text="جارِ الدخول..."
                       FontSize="12"
                       Foreground="#666666"
                       HorizontalAlignment="Center"
                       Margin="0,8,0,0"
                       Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}" />
        </StackPanel>

        <StackPanel VerticalAlignment="Bottom">
            <TextBlock Text="{Binding ErrorMessage}"
                       Foreground="#C0392B"
                       TextWrapping="Wrap"
                       HorizontalAlignment="Center"
                       TextAlignment="Center" />
            <TextBlock Text="{Binding InfoMessage}"
                       Foreground="#2E7D32"
                       TextWrapping="Wrap"
                       HorizontalAlignment="Center"
                       TextAlignment="Center"
                       Margin="0,4,0,0" />
        </StackPanel>
    </Grid>
</Window>

<UserControl x:Class="QalatAldhaman.Store.Admin.Views.ContractsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             FontFamily="Segoe UI">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="عقود طرق الدفع"
                   FontSize="22"
                   FontWeight="Bold"
                   Margin="0,0,0,20" />

        <Grid Grid.Row="1">
            <TextBlock Text="جارِ التحميل..."
                       Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontSize="16"
                       Foreground="#666" />

            <StackPanel Visibility="{Binding IsError, Converter={StaticResource BoolToVis}}"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center">
                <TextBlock Text="{Binding ErrorMessage}"
                           Foreground="#C0392B"
                           Margin="0,0,0,12"
                           TextAlignment="Center"
                           TextWrapping="Wrap"
                           MaxWidth="400" />
                <Button Content="إعادة المحاولة"
                        Command="{Binding LoadCommand}"
                        Padding="16,8"
                        HorizontalAlignment="Center" />
            </StackPanel>

            <StackPanel Visibility="{Binding IsContentVisible, Converter={StaticResource BoolToVis}}">
                <Grid Margin="12,0,12,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="220" />
                        <ColumnDefinition Width="140" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="طريقة الدفع" FontWeight="Bold" Foreground="#555555" />
                    <TextBlock Grid.Column="1" Text="حالة الملف" FontWeight="Bold" Foreground="#555555" />
                </Grid>

                <ItemsControl ItemsSource="{Binding Contracts}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#DDDDDD" BorderThickness="0,0,0,1" Padding="12,16">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="220" />
                                        <ColumnDefinition Width="140" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0" Text="{Binding DisplayName}" VerticalAlignment="Center" />
                                    <TextBlock Grid.Column="1" Text="{Binding StatusText}" VerticalAlignment="Center" />

                                    <Button Grid.Column="2"
                                            Content="رفع/تحديث"
                                            Command="{Binding UploadCommand}"
                                            Padding="12,6"
                                            Margin="0,0,12,0" />

                                    <Button Grid.Column="3"
                                            Content="فتح"
                                            Command="{Binding OpenCommand}"
                                            Padding="12,6"
                                            Margin="0,0,12,0"
                                            Visibility="{Binding HasFile, Converter={StaticResource BoolToVis}}" />

                                    <StackPanel Grid.Column="4" VerticalAlignment="Center">
                                        <TextBlock Text="جارِ الرفع..."
                                                   Visibility="{Binding IsUploading, Converter={StaticResource BoolToVis}}"
                                                   FontSize="11"
                                                   Foreground="#666666" />
                                        <TextBlock Text="{Binding ErrorMessage}"
                                                   Foreground="#C0392B"
                                                   FontSize="11"
                                                   TextWrapping="Wrap" />
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
